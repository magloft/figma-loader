{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Webpack",
      "cwd": "${workspaceFolder}/test",
      "outputCapture": "std",
      "program": "${workspaceFolder}/test/node_modules/.bin/webpack",
      "args": ["-d", "--no-cache"]
    }
  ]
}
